<h1>Todo List</h1>

<%= form_with model: Todo.new, url: todos_path do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>
  <%= form.submit "Add" %>
<% end %>

<ul>
  <% @todos.each do |todo| %>
    <li><%= todo.title %></li>
  <% end %>
</ul>
